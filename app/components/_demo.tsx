///<reference path="../../_references.ts"/>

module NgAuctions.Components {
    interface IXProps {
    }

    interface IXState {

    }

    export class X extends React.Component<IXProps, IXState> {

        componentWillMount():void {
            
        }

        componentDidMount():void {
        }

        componentWillReceiveProps(nextProps:IXProps, nextContext:any):void {
        }

        shouldComponentUpdate(nextProps:IXProps, nextState:IXState, nextContext:any):boolean {
            return null;
        }

        componentWillUpdate(nextProps:IXProps, nextState:IXState, nextContext:any):void {
        }

        componentDidUpdate(prevProps:IXProps, prevState:IXState, prevContext:any):void {
        }

        componentWillUnmount():void {
        }


        public render():JSX.Element {
            return null;
        }

    }
}